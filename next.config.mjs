/** @type {import('next').NextConfig} */
const nextConfig = {
  // uncomment this when deploying
  // output: "export",
  basePath: "/fm-interactive-rating-component",
  assetPrefix: "/fm-interactive-rating-component",
};

export default nextConfig;
